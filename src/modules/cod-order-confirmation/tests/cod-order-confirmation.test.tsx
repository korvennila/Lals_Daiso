/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */

import { buildMockModuleProps } from '@msdyn365-commerce/core-internal';
/// <reference types="jest" />

import * as React from 'react';
import * as renderer from 'react-test-renderer';

import CodOrderConfirmation from '../cod-order-confirmation';
import { ICodOrderConfirmationData } from '../cod-order-confirmation.data';
import { ICodOrderConfirmationConfig, ICodOrderConfirmationProps } from '../cod-order-confirmation.props.autogenerated';

const mockData: ICodOrderConfirmationData = {
    actionResponse: {
        text: 'Sample Response Data'
    }
};

const mockConfig: ICodOrderConfirmationConfig = {
    showText: 'cod-order-confirmation'
};

const mockActions = {};

describe('CodOrderConfirmation', () => {
    let moduleProps: ICodOrderConfirmationProps<ICodOrderConfirmationData>;
    beforeAll(() => {
        moduleProps = buildMockModuleProps(mockData, mockActions, mockConfig) as ICodOrderConfirmationProps<ICodOrderConfirmationData>;
    });
    it('renders correctly', () => {
        const component: renderer.ReactTestRenderer = renderer.create(<CodOrderConfirmation {...moduleProps} />);
        const tree: renderer.ReactTestRendererJSON | renderer.ReactTestRendererJSON[] | null = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
