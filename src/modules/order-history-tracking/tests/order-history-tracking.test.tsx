/*!
 * Copyright (c) Microsoft Corporation.
 * All rights reserved. See LICENSE in the project root for license information.
 */

import { buildMockModuleProps } from '@msdyn365-commerce/core-internal';
/// <reference types="jest" />

import * as React from 'react';
import * as renderer from 'react-test-renderer';

import OrderHistoryTracking from '../order-history-tracking';
import { IOrderHistoryTrackingData } from '../order-history-tracking.data';
import { IOrderHistoryTrackingConfig, IOrderHistoryTrackingProps } from '../order-history-tracking.props.autogenerated';

const mockData: IOrderHistoryTrackingData = {
    actionResponse: {
        text: 'Sample Response Data'
    }
};

const mockConfig: IOrderHistoryTrackingConfig = {
    showText: 'order-history-tracking'
};

const mockActions = {};

describe('OrderHistoryTracking', () => {
    let moduleProps: IOrderHistoryTrackingProps<IOrderHistoryTrackingData>;
    beforeAll(() => {
        moduleProps = buildMockModuleProps(mockData, mockActions, mockConfig) as IOrderHistoryTrackingProps<IOrderHistoryTrackingData>;
    });
    it('renders correctly', () => {
        const component: renderer.ReactTestRenderer = renderer.create(<OrderHistoryTracking {...moduleProps} />);
        const tree: renderer.ReactTestRendererJSON | renderer.ReactTestRendererJSON[] | null = component.toJSON();
        expect(tree).toMatchSnapshot();
    });
});
